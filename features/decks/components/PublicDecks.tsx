import { Typography } from "@mui/material";

const PublicDecks = () => {
    return (
        <Typography variant="h2" component="h2">
            Public Decks
        </Typography>);
};

export default PublicDecks;